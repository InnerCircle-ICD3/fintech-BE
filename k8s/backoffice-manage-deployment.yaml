apiVersion: apps/v1
kind: Deployment
metadata:
  name: backoffice-manage
  labels:
    app: backoffice-manage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backoffice-manage
  template:
    metadata:
      labels:
        app: backoffice-manage
    spec:
      containers:
      - name: backoffice-manage
        image: your-registry-url/backoffice-manage:latest
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: backoffice-manage-service
spec:
  selector:
    app: backoffice-manage
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP 